export class Terminal {
	//
}

/*

cursor
	.to(x, y)
	.move(x, y)
	.up(count = 1)
	.down(count = 1)
	.forward(count = 1)
	.backward(count = 1)
	.nextLine(count = 1)
	.prevLine(count = 1)
	.left
	.hide
	.show
	.save
	.restore
	.get
scroll
	.up(count = 1)
	.down(count = 1)
erase
	.screen
	.up(count = 1)
	.down(count = 1)
	.line
	.lineEnd
	.lineStart
	.lines(count)
clear
	.screen
beep
link(text, url)

widgets
	barChart
	canvas
	gauge
	image
	list
	markdown
	plot
	progress
	prompt
	sparkline
	spinner
	stackedBarChar
	table
	tabs
	tree

*/
